{{#each ideas}}
    <div class="card card-body mb-2">
        <h4>{{title}}</h4>
        <p>{{details}}</p>
        <a class="btn btn-dark" href="/ideas/edit/{{id}}">Edit</a>
    </div>
{{else}}
<p class="">No video ideas listed</p>
{{/each}}